# TODO

## Calendar + UI cleanup
- [x] Fix booking date selector from vertical column to 7-column grid
- [ ] Replace booking date selector with a real month/year calendar (prev/next + correct weekday alignment)
- [ ] Prevent UI overlap across frontend (especially search/headers)
- [ ] Remove profile image in top-right; replace with initials avatar

